<h2>Past Transactions</h2>
<% flash.each do |type, msg| %>
  <div class='alert'>
    <%= msg %>
  </div>
<% end %>

<table class="table">
<thead>
  <tr>
      <th scope="col">Date</th>
  </tr>
  </thead>
  <tbody>
    <%=render(@transactions)%>
  </tbody>
</table>
